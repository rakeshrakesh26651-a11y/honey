# Razorpay API Credentials (TEST MODE or LIVE MODE)
# Server-side environment variables for Vercel and local development
# NEVER prefix with VITE_ to prevent leaking the secret to client-side bundles

RAZORPAY_KEY_ID=rzp_test_your_key_id_here
RAZORPAY_KEY_SECRET=your_key_secret_here

# Optional: Razorpay Webhook Secret (if webhook reconciliation is configured)
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_here
